.media
  .media-left
    = link_to image_tag(comment.user.avatar, size: "64x64"), user_path(@prototype.user)
  .media-body
    %h4#top-aligned-media.media-heading
      = link_to "#{comment.user.name}", user_path(comment.user), class: "anchorjs-link"
      %p
        = comment.content